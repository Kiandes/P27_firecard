import {MarkingProps} from 'react-native-calendars/src/calendar/day/marking';

/**
 * Type for markedDates props for react-native-calendar.
 */
type MarkedDatesProps = {
  [key: string]: MarkingProps;
};

export default MarkedDatesProps;
